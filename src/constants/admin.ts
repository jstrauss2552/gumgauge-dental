/** Session key used to indicate admin is logged in (sessionStorage). */
export const ADMIN_SESSION_KEY = "gumgauge-admin-session";

/** Demo admin password. In production, replace with proper auth. */
export const ADMIN_PASSWORD = "gumgauge";
